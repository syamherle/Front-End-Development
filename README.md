Simple login portal using angular js and php
Steps to setup
Download all the file and place in a localhost server (Wamp)
Set username as "root" and password as "root" for phpmyadmin
Set database as "portaldb"
